"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const node_path_1 = __importDefault(require("node:path"));
const jokeAction_1 = __importDefault(require("../jokeAction"));
const internet_utils_1 = require("../../../utils/internet.utils");
const joke_1 = __importDefault(require("../joke"));
// Mock des dépendances
// Mock de GeminiProvider
jest.mock("../../../services/ai/gemini", () => {
    return {
        __esModule: true,
        default: {
            // ajoute getProvider comme dans ton code
            getProvider: () => ({
                textToText: jest.fn().mockResolvedValue(JSON.stringify({
                    setup: "Setup mock",
                    punchline: "Punchline mock",
                    options: { theme: "mock" },
                })),
            }),
        },
    };
});
jest.mock("../../../utils/internet.utils");
describe("JokeAction", () => {
    const jokeAction = jokeAction_1.default.getAction();
    const localFile = node_path_1.default.join(__dirname, "data.json");
    beforeEach(() => {
        jest.clearAllMocks();
    });
    it("doit retourner une instance singleton", () => {
        const anotherInstance = jokeAction_1.default.getAction();
        expect(jokeAction).toBe(anotherInstance);
    });
    it("Doit pouvoir genérer une blague sans options", async () => {
        internet_utils_1.checkConnection.mockResolvedValue(true);
        const joke = await jokeAction.generate();
        expect(joke).toBeDefined();
        expect(joke).toBeInstanceOf(joke_1.default);
    });
    it("doit retourner un joke local si pas de connexion", async () => {
        internet_utils_1.checkConnection.mockResolvedValue(false);
        const joke = await jokeAction.generate({ theme: "dark" });
        expect(joke).toBeDefined();
        expect(joke).toBeInstanceOf(joke_1.default);
    });
    it("doit retourner un joke de l'IA si connecté", async () => {
        internet_utils_1.checkConnection.mockResolvedValue(true);
        const joke = (await jokeAction.generate({ theme: "funny" })).toJSON();
        expect(joke.setup).toBe("Setup mock");
        expect(joke.punchline).toBe("Punchline mock");
    });
});
